// MiniProfile.jsx
// 🔝 Member 1 — build this component
// This is a placeholder so the app doesn't crash

export default function MiniProfile() {
  return <div>MiniProfile — Member 1 will build this</div>;
}